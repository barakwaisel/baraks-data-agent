# Memory

Long-lived notes the agent maintains across sessions. The agent may append
dated bullets here when it learns something durable (naming conventions,
recurring questions, preferences).

- 2026-07-15: Workspace created. Primary dataset: `my_app` (BigQuery sandbox);
  main table `user_clicks` (~10k synthetic click events, 200 fake users).
